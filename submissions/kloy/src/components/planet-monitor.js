import React, { createClass } from 'react';

const PlanetMonitor = createClass({
    displayName: 'PlanetMonitor',

    render() {
        return (
            <h1 className="css-planet-monitor">Obi-Wan currently on Tatooine</h1>
        );
    }
});

export default PlanetMonitor;
